#!/usr/bin/python3

def add(x, y):
  return x + y


def main():
    x = int(input("Input x: "))
    y = int(input("Input y: "))

    # returns x + y
    addition = add(x, y)
    print(addition)

    # sub(x, y) returns x - y

    # mult(x, y) returns x * y

    # div(x, y) returns x // y

    # mod(x, y) returns x % y

    # expo(x, y) returns x ** y

if __name__ == "__main__":
  	main()

